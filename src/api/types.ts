export type PokemonSprites = {
    sprites: {
        other: {
            'official-artwork': {
                'front_default': string
            }
        }
    }
}
export type PokemonType = {
    type: { name: string };
}